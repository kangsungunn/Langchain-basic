# 🚀 다음 단계 전략 (2026-01-28)

## ✅ 현재 완료된 작업

### 1. LangGraph 오케스트레이터
- [x] 상태 스키마 정의 (`WorkflowState`)
- [x] LangGraph 노드 구현 (validate, determine_strategy, policy_process, rule_process, finalize)
- [x] LangGraph 그래프 빌드 및 컴파일
- [x] WorkflowManager LangGraph 전환 완료
- [x] 테스트 통과 (규칙 기반/정책 기반 모두 성공)
- [x] 이모지 제거로 로깅 에러 수정 완료

### 2. KoELECTRA 파인튜닝 구조
- [x] 더미 데이터 생성 스크립트 (`data_factory.py`)
- [x] 실제 데이터 기준 학습 스크립트 (`train_koelectra_policy_rule.py`)
- [x] 더미 데이터로 학습 완료 (3 에폭)
- [x] 파인튜닝된 모델 저장 완료 (`model.safetensors`)
- [x] KoELECTRALoader 자동 모델 선택 구현
- [x] 파인튜닝된 모델 자동 감지 및 사용

---

## 🎯 다음 단계 옵션

### 옵션 1: 파인튜닝된 모델 통합 및 검증 (권장)

**목적**: 파인튜닝된 모델이 실제로 사용되는지 확인하고 성능 검증

**작업**:
1. 파인튜닝된 모델 사용 확인 테스트 (`test_koelectra_finetuned.py` 실행)
2. 전체 플로우 테스트 (`test_langgraph_orchestrator.py` 실행)
3. 베이스 모델 vs 파인튜닝 모델 성능 비교
4. 판단 정확도 측정 및 개선

**예상 소요 시간**: 1-2일

**우선순위**: 높음 (현재 구조 완성 상태)

---

### 옵션 2: 실제 데이터 준비 및 학습

**목적**: 더미 데이터를 실제 데이터로 교체하여 실제 성능 향상

**작업**:
1. 실제 API 요청 로그 수집
   - DecisionMaker 호출 로그 수집
   - 도메인/액션/요청 데이터 기록
   - 정책/규칙 라벨 수동 또는 자동 생성
2. 데이터 라벨링
   - 정책 기반: `label: 0` (policy)
   - 규칙 기반: `label: 1` (rule)
3. JSONL 형식으로 변환
   - `training/data/policy_rule_classification/train.jsonl`
   - `training/data/policy_rule_classification/val.jsonl`
   - `training/data/policy_rule_classification/test.jsonl`
4. 실제 데이터로 학습 (5-10 에폭)
5. 성능 평가 및 비교

**예상 소요 시간**: 3-5일 (데이터 수집 및 라벨링 포함)

**우선순위**: 중간 (구조는 완성, 실제 데이터 필요)

---

### 옵션 3: 다른 기능 개발

#### 3-1. PDF 파싱 기능 (학습용 데이터셋)
- **목적**: PDF 파일을 자동으로 파싱하여 학습 데이터 생성
- **작업**: PDF 텍스트 추출, 문제/답안 자동 분리
- **우선순위**: 중간

#### 3-2. OCR 기능 (사용자 답안지)
- **목적**: 이미지 기반 답안지를 텍스트로 변환
- **작업**: Tesseract 또는 Google Vision API 연동
- **우선순위**: 중간

#### 3-3. 프론트엔드 연동 확인
- **목적**: 프론트엔드에서 오케스트레이터 API 호출 정상 동작 확인
- **작업**: API 엔드포인트 테스트, 에러 처리 확인
- **우선순위**: 높음

---

## 📋 권장 진행 순서

### Phase 1: 통합 및 검증 (1-2일)
1. 파인튜닝된 모델 사용 확인 테스트 실행
2. 전체 플로우 테스트 실행
3. 성능 비교 (베이스 vs 파인튜닝)
4. 판단 정확도 측정

### Phase 2: 실제 데이터 준비 (3-5일)
1. 실제 API 요청 로그 수집 스크립트 작성
2. 데이터 라벨링 도구 또는 자동 라벨링 구현
3. JSONL 변환 및 검증
4. 실제 데이터로 학습 실행

### Phase 3: 성능 최적화 (1-2일)
1. 학습 하이퍼파라미터 튜닝
2. 모델 성능 평가 및 개선
3. 프로덕션 배포 준비

---

## 🔧 즉시 실행 가능한 작업

### 1. 파인튜닝된 모델 사용 확인
```bash
python test_koelectra_finetuned.py
```

### 2. 전체 플로우 테스트
```bash
python test_langgraph_orchestrator.py
```

### 3. 실제 데이터 수집 스크립트 작성 (새로 생성)
- API 요청 로그 수집
- 자동 라벨링 (사전 필터링 결과 기반)
- JSONL 변환

---

## ✅ 체크리스트

### 완료
- [x] LangGraph 오케스트레이터 구현
- [x] KoELECTRA 파인튜닝 구조 완성
- [x] 파인튜닝된 모델 자동 감지 및 사용
- [x] 로깅 에러 수정

### 다음 단계 (사용자가 직접 실행)
- [ ] 파인튜닝된 모델 사용 확인 테스트
- [ ] 전체 플로우 테스트
- [ ] 성능 비교 (베이스 vs 파인튜닝)

### 향후 작업
- [ ] 실제 데이터 수집 스크립트 작성
- [ ] 실제 데이터 라벨링
- [ ] 실제 데이터로 학습
- [ ] 성능 평가 및 최적화

---

**작성 일시**: 2026-01-28
**현재 상태**: 구조 완성, 통합 및 검증 단계
