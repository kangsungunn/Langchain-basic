name: Deploy to EC2

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy FastAPI to EC2
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

      - name: Add EC2 to known hosts
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}
        run: |
          if [ -z "$EC2_HOST" ]; then
            echo "❌ Error: EC2_HOST secret is not set"
            exit 1
          fi
          echo "🔍 Adding EC2 host to known_hosts: $EC2_HOST"
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          ssh-keyscan -H "$EC2_HOST" >> ~/.ssh/known_hosts 2>&1
          chmod 600 ~/.ssh/known_hosts
          echo "✅ EC2 host added to known_hosts"

      - name: Deploy to EC2
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}
          EC2_USER: ${{ secrets.EC2_USER }}
        run: |
          if [ -z "$EC2_HOST" ] || [ -z "$EC2_USER" ]; then
            echo "❌ Error: EC2_HOST or EC2_USER secret is not set"
            exit 1
          fi
          echo "🚀 Connecting to $EC2_USER@$EC2_HOST"
          ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=~/.ssh/known_hosts $EC2_USER@$EC2_HOST << 'ENDSSH'
            set -e

            echo "🚀 Starting deployment..."

            # 애플리케이션 디렉토리로 이동
            cd /var/www/langchain

            # Git pull
            if [ -d ".git" ]; then
              echo "📥 Pulling latest changes..."
              git pull origin main
            else
              echo "📥 Cloning repository..."
              cd /var/www
              sudo rm -rf langchain
              sudo mkdir langchain
              sudo chown ubuntu:ubuntu langchain
              cd langchain
              git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git .
            fi

            # 가상 환경 활성화
            source venv/bin/activate

            # 의존성 설치
            echo "📦 Installing dependencies..."
            pip install -r requirements.txt

            # 서비스 재시작
            echo "🔄 Restarting service..."
            sudo systemctl restart langchain-api.service

            # 서비스 상태 확인
            sleep 3
            if sudo systemctl is-active --quiet langchain-api.service; then
              echo "✅ Deployment successful!"
              sudo systemctl status langchain-api.service --no-pager
            else
              echo "❌ Deployment failed!"
              sudo journalctl -u langchain-api.service -n 50 --no-pager
              exit 1
            fi
          ENDSSH

      - name: Health Check
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}
        run: |
          if [ -z "$EC2_HOST" ]; then
            echo "❌ Error: EC2_HOST secret is not set"
            exit 1
          fi
          echo "🏥 Performing health check on http://$EC2_HOST/health"
          sleep 5
          response=$(curl -s -o /dev/null -w "%{http_code}" "http://$EC2_HOST/health" || echo "000")
          if [ "$response" -eq 200 ]; then
            echo "✅ Health check passed!"
          else
            echo "❌ Health check failed with status $response"
            exit 1
          fi

      - name: Notify Deployment Status
        if: always()
        run: |
          if [ ${{ job.status }} == 'success' ]; then
            echo "🎉 Deployment completed successfully!"
          else
            echo "❌ Deployment failed!"
          fi
