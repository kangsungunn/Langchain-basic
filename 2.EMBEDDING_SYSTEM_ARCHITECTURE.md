# 임베딩 시스템 아키텍처 가이드

## 📋 목차

1. [시스템 개요](#시스템-개요)
2. [전체 아키텍처](#전체-아키텍처)
3. [주요 구성 요소](#주요-구성-요소)
4. [데이터 흐름](#데이터-흐름)
5. [폴더 구조 및 역할](#폴더-구조-및-역할)
6. [기술 스택](#기술-스택)
7. [실행 흐름 상세 설명](#실행-흐름-상세-설명)

---

## 시스템 개요

이 시스템은 **선수 데이터를 벡터 임베딩으로 변환하여 저장**하는 배치 작업 시스템입니다.

### 핵심 개념

- **임베딩(Embedding)**: 텍스트를 숫자 벡터로 변환하는 기술
- **배치 작업(Batch Job)**: 대량의 데이터를 한 번에 처리하는 작업
- **작업 큐(Job Queue)**: 작업을 순서대로 처리하기 위한 대기열
- **비동기 처리**: 작업을 백그라운드에서 처리하여 사용자 응답 속도 향상

### 왜 이런 구조를 사용하나요?

1. **사용자 경험**: 임베딩 생성은 시간이 오래 걸리므로, 사용자가 기다리지 않고 다른 작업을 할 수 있게 함
2. **안정성**: 작업이 실패해도 재시도 가능
3. **확장성**: 여러 워커를 실행하여 작업 처리 속도 향상 가능

---

## 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                         사용자 (브라우저)                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 1. "임베딩 생성" 버튼 클릭
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    프론트엔드 (Next.js)                          │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  frontend/app/v10/admin/upload/player/page.tsx          │   │
│  │  - 사용자 인터페이스 제공                                │   │
│  │  - 작업 요청 전송                                        │   │
│  │  - 작업 상태 실시간 확인                                 │   │
│  └──────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 2. HTTP GET /api/v10/soccer/player/embedding
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    백엔드 API 서버 (FastAPI)                     │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  app/api/v10/soccer/player_router.py                    │   │
│  │  - API 엔드포인트 제공                                   │   │
│  │  - 작업을 BullMQ 큐에 추가                               │   │
│  │  - 작업 ID 반환                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                             │                                    │
│                             │ 3. 작업을 큐에 추가                │
│                             ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  app/api/shared/redis.py                                 │   │
│  │  - Redis 연결 관리                                        │   │
│  │  - BullMQ 작업 큐 관리                                    │   │
│  └──────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 4. 작업이 큐에 저장됨
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Upstash Redis (클라우드)                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  작업 큐 (embedding:wait)                                │   │
│  │  - 대기 중인 작업들 저장                                  │   │
│  │  - 작업 상태 관리 (waiting, active, completed, failed)  │   │
│  └──────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 5. 워커가 작업을 가져옴
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    임베딩 워커 (별도 프로세스)                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  app/api/shared/embedding_worker.py                      │   │
│  │  - 큐에서 작업 가져오기                                   │   │
│  │  - 선수 데이터 조회                                      │   │
│  │  - 임베딩 생성                                           │   │
│  │  - 데이터베이스에 저장                                    │   │
│  └──────────────────────────────────────────────────────────┘   │
│                             │                                    │
│                             │ 6. 임베딩 모델 사용                │
│                             ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  app/domain/v10/soccer/spokes/infrastructure/            │   │
│  │  embedding_client.py                                     │   │
│  │  - 로컬 Hugging Face 모델 로드                            │   │
│  │  - 텍스트를 벡터로 변환                                   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                             │                                    │
│                             │ 7. 데이터베이스에 저장             │
│                             ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  app/domain/v10/soccer/spokes/services/                  │   │
│  │  player_service.py                                       │   │
│  │  - 비즈니스 로직 처리                                     │   │
│  │  - 데이터베이스 저장                                      │   │
│  └──────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 8. 작업 완료 상태 업데이트
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    PostgreSQL 데이터베이스                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  players 테이블                                           │   │
│  │  - 선수 기본 정보                                         │   │
│  │                                                            │   │
│  │  player_embeddings 테이블                                 │   │
│  │  - 선수별 임베딩 벡터 (768차원)                           │   │
│  │  - 원본 텍스트                                            │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 주요 구성 요소

### 1. 프론트엔드 (Frontend)

**역할**: 사용자 인터페이스 제공 및 작업 상태 모니터링

**주요 파일**:
- `frontend/app/v10/admin/upload/player/page.tsx`
  - 사용자가 "임베딩 생성" 버튼을 클릭하는 UI
  - 작업 요청을 백엔드로 전송
  - 작업 상태를 주기적으로 확인 (폴링)

**동작 방식**:
1. 사용자가 버튼 클릭
2. 백엔드 API 호출 → 작업 ID 받음
3. 2초마다 작업 상태 확인
4. 완료/실패 시 사용자에게 알림

### 2. 백엔드 API (Backend API)

**역할**: 작업 요청을 받아 큐에 추가

**주요 파일**:
- `app/api/v10/soccer/player_router.py`
  - `/embedding` 엔드포인트: 작업을 큐에 추가
  - `/embedding/status/{job_id}` 엔드포인트: 작업 상태 조회

**동작 방식**:
1. 프론트엔드로부터 요청 받음
2. 작업 데이터 생성
3. Redis 큐에 작업 추가
4. 작업 ID 반환

### 3. Redis 큐 (Redis Queue)

**역할**: 작업을 저장하고 관리하는 대기열

**주요 파일**:
- `app/api/shared/redis.py`
  - Redis 연결 관리
  - BullMQ 작업 큐 관리 함수들

**큐 구조**:
```
embedding:wait      → 대기 중인 작업들
embedding:active    → 현재 처리 중인 작업들
embedding:completed → 완료된 작업들
embedding:failed    → 실패한 작업들
embedding:{job_id}  → 작업 상세 정보
```

### 4. 임베딩 워커 (Embedding Worker)

**역할**: 큐에서 작업을 가져와 실제 임베딩 생성 및 저장

**주요 파일**:
- `app/api/shared/embedding_worker.py`
  - 큐에서 작업 가져오기
  - 선수 데이터 조회
  - 임베딩 생성
  - 데이터베이스 저장

**동작 방식**:
1. 5초마다 큐 확인
2. 작업이 있으면 가져오기
3. 선수 데이터 조회
4. 각 선수에 대해:
   - 텍스트 생성 (이름, 포지션, 등번호 등)
   - 임베딩 생성
   - 데이터베이스에 저장
5. 100개마다 중간 커밋
6. 완료 시 최종 커밋

### 5. 임베딩 클라이언트 (Embedding Client)

**역할**: 로컬 Hugging Face 모델을 사용하여 임베딩 생성

**주요 파일**:
- `app/domain/v10/soccer/spokes/infrastructure/embedding_client.py`
  - 로컬 모델 로드
  - 텍스트를 벡터로 변환

**사용 모델**:
- `jhgan/ko-sroberta-multitask` (768차원, 한국어 특화)
- `artifacts/embedding-models/` 폴더에 저장

### 6. 서비스 레이어 (Service Layer)

**역할**: 비즈니스 로직 처리

**주요 파일**:
- `app/domain/v10/soccer/spokes/services/player_service.py`
  - 선수 데이터 정규화
  - 데이터베이스 저장
  - 배치 임베딩 인덱싱

---

## 데이터 흐름

### 시퀀스 다이어그램

```
사용자          프론트엔드          백엔드 API        Redis 큐        워커          DB
  │                │                  │                │              │            │
  │──버튼 클릭──>│                  │                │              │            │
  │                │                  │                │              │            │
  │                │──작업 요청─────>│                │              │            │
  │                │                  │                │              │            │
  │                │                  │──큐에 추가───>│              │            │
  │                │                  │                │              │            │
  │                │<──작업 ID───────│                │              │            │
  │                │                  │                │              │            │
  │                │──상태 조회──────>│                │              │            │
  │                │                  │──상태 확인───>│              │            │
  │                │                  │<──상태────────│              │            │
  │                │<──상태───────────│                │              │            │
  │                │                  │                │              │            │
  │                │                  │                │<──작업 가져오기│            │
  │                │                  │                │              │            │
  │                │                  │                │              │──데이터 조회─>│
  │                │                  │                │              │<──선수 데이터─│
  │                │                  │                │              │            │
  │                │                  │                │              │──임베딩 생성─│
  │                │                  │                │              │            │
  │                │                  │                │              │──저장──────>│
  │                │                  │                │              │            │
  │                │                  │                │<──완료───────│            │
  │                │                  │                │              │            │
  │                │──상태 조회──────>│                │              │            │
  │                │                  │──상태 확인───>│              │            │
  │                │                  │<──완료────────│              │            │
  │                │<──완료───────────│                │              │            │
  │<──완료 알림────│                  │                │              │            │
```

### 단계별 상세 설명

#### 1단계: 작업 요청
```
사용자 → 프론트엔드: "임베딩 생성" 버튼 클릭
프론트엔드 → 백엔드: GET /api/v10/soccer/player/embedding
백엔드 → Redis: 작업을 큐에 추가
백엔드 → 프론트엔드: 작업 ID 반환
```

#### 2단계: 상태 확인 (폴링)
```
프론트엔드 → 백엔드: GET /api/v10/soccer/player/embedding/status/{job_id}
백엔드 → Redis: 작업 상태 조회
Redis → 백엔드: 상태 반환 (waiting/active/completed/failed)
백엔드 → 프론트엔드: 상태 반환
```

#### 3단계: 작업 처리
```
워커 → Redis: 큐에서 작업 가져오기
워커 → DB: 선수 데이터 조회
워커 → 임베딩 모델: 텍스트를 벡터로 변환
워커 → DB: 임베딩 저장
워커 → Redis: 작업 상태를 "completed"로 업데이트
```

---

## 폴더 구조 및 역할

```
RAG/
├── frontend/                          # 프론트엔드 (Next.js)
│   └── app/
│       └── v10/
│           └── admin/
│               └── upload/
│                   └── player/
│                       └── page.tsx   # 📱 사용자 인터페이스
│                                       # - 임베딩 생성 버튼
│                                       # - 작업 상태 표시
│                                       # - 상태 폴링 로직
│
├── app/                               # 백엔드 (FastAPI)
│   ├── api/
│   │   ├── v10/
│   │   │   └── soccer/
│   │   │       └── player_router.py   # 🔌 API 엔드포인트
│   │   │                               # - /embedding: 작업 요청
│   │   │                               # - /embedding/status/{job_id}: 상태 조회
│   │   │
│   │   └── shared/
│   │       ├── redis.py               # 🔄 Redis/BullMQ 관리
│   │       │                           # - Redis 연결
│   │       │                           # - 작업 큐 추가/조회
│   │       │                           # - 작업 상태 관리
│   │       │
│   │       └── embedding_worker.py    # ⚙️ 임베딩 워커
│   │                                   # - 큐에서 작업 가져오기
│   │                                   # - 선수 데이터 처리
│   │                                   # - 임베딩 생성 및 저장
│   │
│   └── domain/
│       └── v10/
│           └── soccer/
│               └── spokes/
│                   ├── infrastructure/
│                   │   └── embedding_client.py  # 🤖 임베딩 모델 클라이언트
│                   │                             # - 로컬 모델 로드
│                   │                             # - 텍스트 → 벡터 변환
│                   │
│                   └── services/
│                       └── player_service.py     # 💼 비즈니스 로직
│                                                 # - 데이터 정규화
│                                                 # - 데이터베이스 저장
│                                                 # - 배치 처리
│
└── artifacts/                         # 모델 저장소
    └── embedding-models/              # 📦 임베딩 모델
        └── jhgan--ko-sroberta-multitask/
                                        # - Hugging Face 모델 파일들
                                        # - 자동 다운로드됨
```

### 파일별 역할 요약

| 파일 경로 | 역할 | 주요 기능 |
|---------|------|----------|
| `frontend/app/v10/admin/upload/player/page.tsx` | 사용자 인터페이스 | 버튼 클릭, 상태 표시, 폴링 |
| `app/api/v10/soccer/player_router.py` | API 엔드포인트 | 작업 요청 받기, 상태 조회 |
| `app/api/shared/redis.py` | 큐 관리 | Redis 연결, 작업 추가/조회 |
| `app/api/shared/embedding_worker.py` | 작업 처리 | 큐에서 작업 가져와 처리 |
| `app/domain/.../embedding_client.py` | 임베딩 생성 | 텍스트를 벡터로 변환 |
| `app/domain/.../player_service.py` | 비즈니스 로직 | 데이터 처리 및 저장 |

---

## 기술 스택

### 프론트엔드
- **Next.js**: React 기반 프레임워크
- **TypeScript**: 타입 안정성
- **Tailwind CSS**: 스타일링

### 백엔드
- **FastAPI**: Python 웹 프레임워크
- **SQLAlchemy**: 데이터베이스 ORM
- **PostgreSQL**: 관계형 데이터베이스
- **pgvector**: 벡터 데이터 저장

### 큐 시스템
- **Upstash Redis**: 클라우드 Redis 서비스
- **BullMQ**: 작업 큐 시스템 (직접 구현)

### 머신러닝
- **Hugging Face**: 모델 라이브러리
- **sentence-transformers**: 임베딩 모델
- **jhgan/ko-sroberta-multitask**: 한국어 특화 모델

---

## 실행 흐름 상세 설명

### 1. 사용자가 "임베딩 생성" 버튼 클릭

**위치**: `frontend/app/v10/admin/upload/player/page.tsx`

```typescript
// 사용자가 버튼을 클릭하면
const handleEmbedding = async () => {
  // 1. 로딩 상태로 변경
  setIsEmbedding(true)

  // 2. 백엔드 API 호출
  const response = await fetch(`${apiUrl}/api/v10/soccer/player/embedding`)

  // 3. 작업 ID 받기
  const data = await response.json()
  const jobId = data.job_id

  // 4. 상태 폴링 시작
  pollEmbeddingStatus(jobId)
}
```

**무슨 일이 일어나나요?**
- 프론트엔드가 백엔드에 "임베딩 작업을 시작해주세요" 요청
- 백엔드는 즉시 응답하고 작업 ID 반환
- 사용자는 기다리지 않고 다른 작업 가능

### 2. 백엔드가 작업을 큐에 추가

**위치**: `app/api/v10/soccer/player_router.py`

```python
@router.get("/embedding")
async def generate_embeddings():
    # 1. 작업 데이터 생성
    job_data = {
        "name": "generate_player_embeddings",
        "type": "embedding",
        "entity": "player"
    }

    # 2. 큐에 작업 추가
    job_id = add_bullmq_job(
        queue_name="embedding",
        job_data=job_data
    )

    # 3. 작업 ID 반환
    return {"job_id": job_id, "status": "queued"}
```

**무슨 일이 일어나나요?**
- 백엔드가 작업 정보를 생성
- Redis 큐에 작업 추가
- 작업 ID를 프론트엔드에 반환

### 3. 워커가 작업을 가져옴

**위치**: `app/api/shared/embedding_worker.py`

```python
async def process_queue():
    while True:
        # 1. 큐에서 작업 가져오기
        job_id = redis.rpop("embedding:wait")

        if job_id:
            # 2. 작업 처리
            await process_embedding_job(job_data)
```

**무슨 일이 일어나나요?**
- 워커가 5초마다 큐 확인
- 작업이 있으면 가져와서 처리 시작
- 작업 상태를 "active"로 변경

### 4. 임베딩 생성 및 저장

**위치**: `app/api/shared/embedding_worker.py`

```python
async def process_embedding_job():
    # 1. 모든 선수 데이터 조회
    players = await session.execute(select(Player))

    for player in players:
        # 2. 텍스트 생성
        content = create_player_content(player)
        # 예: "손흥민, Son Heung-min, 공격수, 대한민국, 등번호 7"

        # 3. 임베딩 생성
        vector = await embeddings_model.embed_query(content)
        # 예: [0.1, 0.2, 0.3, ..., 0.9] (768개 숫자)

        # 4. 데이터베이스에 저장
        embedding = PlayerEmbedding(
            player_id=player.id,
            content=content,
            embedding=vector
        )
        session.add(embedding)
```

**무슨 일이 일어나나요?**
- 각 선수에 대해:
  1. 선수 정보를 텍스트로 변환
  2. 임베딩 모델로 벡터 생성
  3. 데이터베이스에 저장
- 100개마다 중간 저장 (안정성)
- 모든 작업 완료 시 최종 저장

### 5. 프론트엔드가 상태 확인 (폴링)

**위치**: `frontend/app/v10/admin/upload/player/page.tsx`

```typescript
const pollEmbeddingStatus = async (jobId: string) => {
  const poll = async () => {
    // 1. 상태 조회
    const response = await fetch(
      `${apiUrl}/api/v10/soccer/player/embedding/status/${jobId}`
    )
    const data = await response.json()

    // 2. 상태에 따라 처리
    if (data.status === 'completed') {
      // 완료: 성공 메시지 표시
      setEmbeddingStatus('success')
    } else if (data.status === 'active') {
      // 진행 중: 2초 후 다시 확인
      setTimeout(poll, 2000)
    }
  }

  poll() // 첫 확인 시작
}
```

**무슨 일이 일어나나요?**
- 프론트엔드가 2초마다 작업 상태 확인
- 상태가 "completed"면 성공 메시지 표시
- 상태가 "active"면 계속 확인

---

## 주요 개념 설명

### 1. 비동기 처리 (Asynchronous Processing)

**왜 필요한가요?**
- 임베딩 생성은 시간이 오래 걸림 (수백~수천 명의 선수)
- 사용자가 기다리면 안 좋은 경험

**해결 방법:**
- 작업을 큐에 넣고 즉시 응답
- 백그라운드에서 처리
- 상태를 주기적으로 확인

### 2. 작업 큐 (Job Queue)

**왜 필요한가요?**
- 여러 작업을 순서대로 처리
- 작업 실패 시 재시도 가능
- 작업 상태 추적 가능

**동작 방식:**
```
대기 큐 → 활성 큐 → 완료 큐
         ↓
      실패 큐
```

### 3. 폴링 (Polling)

**왜 필요한가요?**
- 웹소켓 같은 실시간 통신이 없을 때
- 간단하고 안정적

**동작 방식:**
- 주기적으로 상태 확인 (예: 2초마다)
- 상태가 변경되면 UI 업데이트

### 4. 임베딩 (Embedding)

**무엇인가요?**
- 텍스트를 숫자 벡터로 변환
- 의미가 비슷한 텍스트는 비슷한 벡터

**예시:**
```
"손흥민, 공격수" → [0.1, 0.2, 0.3, ..., 0.9]
"이강인, 미드필더" → [0.2, 0.1, 0.4, ..., 0.8]
```

**왜 필요한가요?**
- 벡터 검색으로 유사한 선수 찾기 가능
- RAG(검색 기반 생성)에 활용

---

## 환경 변수 설정

### 프론트엔드 (`.env.local`)

```env
# 백엔드 API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# 프론트엔드 URL (선택사항)
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3000
```

### 백엔드 (`.env`)

```env
# Redis 설정
UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-token

# 데이터베이스 설정
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=langchain_db
POSTGRES_USER=langchain_user
POSTGRES_PASSWORD=langchain_password

# 임베딩 모델 설정
EMBEDDING_MODEL_NAME=jhgan/ko-sroberta-multitask
EMBEDDING_DEVICE=cpu  # GPU 사용 시 "cuda"
```

---

## 실행 방법

### 1. 백엔드 서버 실행

```bash
cd app
uvicorn main:app --reload
```

### 2. 워커 실행 (별도 터미널)

```bash
cd app
python -m app.api.shared.embedding_worker
```

### 3. 프론트엔드 실행 (별도 터미널)

```bash
cd frontend
npm run dev
```

### 4. 사용

1. 브라우저에서 `http://localhost:3000/v10/admin/upload/player` 접속
2. "임베딩 생성" 버튼 클릭
3. 상태 확인 (대기 중 → 처리 중 → 완료)

---

## 트러블슈팅

### 문제: 작업이 큐에 추가되지 않음

**원인**: Redis 연결 실패
**해결**:
- `UPSTASH_REDIS_REST_URL`과 `UPSTASH_REDIS_REST_TOKEN` 확인
- Redis 서비스가 실행 중인지 확인

### 문제: 워커가 작업을 처리하지 않음

**원인**: 워커가 실행되지 않음
**해결**:
- 워커를 별도 터미널에서 실행
- 로그 확인

### 문제: 임베딩 생성이 실패함

**원인**: 모델 로드 실패 또는 데이터베이스 연결 실패
**해결**:
- 모델이 `artifacts/embedding-models/`에 있는지 확인
- 데이터베이스 연결 설정 확인

---

## 확장 가능성

### 1. 여러 워커 실행

```bash
# 터미널 1
python -m app.api.shared.embedding_worker

# 터미널 2
python -m app.api.shared.embedding_worker

# 터미널 3
python -m app.api.shared.embedding_worker
```

여러 워커를 실행하면 작업 처리 속도가 향상됩니다.

### 2. 다른 엔티티 지원

현재는 선수(Player)만 지원하지만, 팀(Team), 경기장(Stadium) 등도 동일한 방식으로 추가 가능합니다.

### 3. 실시간 업데이트

폴링 대신 WebSocket을 사용하면 실시간으로 상태를 업데이트할 수 있습니다.

---

## 학습 포인트

1. **비동기 처리**: 사용자 경험을 위한 배경 처리
2. **작업 큐**: 대량 작업의 안정적인 처리
3. **상태 관리**: 작업 진행 상황 추적
4. **모듈화**: 각 컴포넌트의 명확한 역할 분리
5. **확장성**: 시스템의 성장 가능성

---

## 참고 자료

- [FastAPI 공식 문서](https://fastapi.tiangolo.com/)
- [Next.js 공식 문서](https://nextjs.org/docs)
- [Hugging Face Transformers](https://huggingface.co/docs/transformers)
- [BullMQ 문서](https://docs.bullmq.io/)

---

**작성일**: 2026-02-03
**버전**: 1.0.0

