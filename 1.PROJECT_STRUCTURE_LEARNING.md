# 프로젝트 전체 구조 및 기능 학습 가이드

> 강사님 코드베이스의 폴더/파일 구조, 역할, 데이터 흐름을 초보자도 따라갈 수 있도록 설명 위주로 정리한 문서입니다.

---

## 1. 프로젝트 개요

이 프로젝트는 **FastAPI 기반 RAG(검색 기반 생성) 백엔드**로, 다음을 담당합니다.

- **축구 도메인**: 선수·팀·경기장·일정 데이터의 업로드, 임베딩, 자연어 질의 응답
- **MCP(Model Context Protocol)**: ExaOne(LLM), KoELECTRA(임베딩) 등 모델을 **툴**로 제공해 오케스트레이터가 호출
- **임베딩**: 선수/팀/일정/경기장 텍스트를 벡터화해 DB에 저장하고, 질의 시 유사 검색에 사용
- **학습/파인튜닝**: KoELECTRA 분류 모델 학습 데이터 생성 및 파인튜닝 (별도 폴더)

**주의**: 폴더 이름 `artfacts-training`은 "artifacts"의 오타로, 학습·훈련 관련 코드가 들어 있습니다.

---

## 2. 루트 폴더별 역할 (한눈에 보기)

| 폴더/파일 | 역할 |
|-----------|------|
| **app/** | 백엔드 애플리케이션 전부 (API, 도메인 로직, DB, MCP, LLM) |
| **artfacts-training/** | KoELECTRA 학습 데이터 생성·파인튜닝·임베딩 채우기 스크립트 |
| **alembic/** | DB 스키마 버전 관리(마이그레이션) |
| **artifacts/** | 코드에서 참조하는 **경로** (실제 디렉터리는 실행 시 생성 가능). 모델·임베딩 모델 저장 위치 |
| **docs/architecture/** | 아키텍처·MCP 구조 분석 문서 |
| **scripts/** | 배치 임베딩 실행, 모델 다운로드, 임베딩 확인, 배포 등 유틸 스크립트 |
| **frontend/** | Next.js 프론트엔드 (이 문서는 백엔드·학습 쪽 위주) |
| **libs/** | LangChain 등 외부 라이브러리 (수정 대상 아님) |
| **md_files/** | 운영·마이그레이션·설정 관련 메모 문서 |

---

## 3. app/ 폴더 상세

백엔드의 핵심입니다. **진입점**은 `app/main.py`이고, 여기서 FastAPI 앱 생성·DB 테스트·라우터 등록을 합니다.

### 3.1 app/main.py

- FastAPI 앱 생성, CORS, 라우터 등록
- 시작 시 DB 연결 테스트, Alembic 마이그레이션 자동 적용
- **라우터 등록**: chat, player, team, stadium, schedule, admin, product 등

### 3.2 app/api/ — HTTP API 계층

- **api/shared/redis.py**  
  Upstash Redis 연동(임베딩 작업 트리거·진행 상태 등).  
  GET `/api/v10/soccer/player/embedding` 호출 시 Redis에 작업을 올리고, 백그라운드에서 MCP 기반 임베딩 배치가 돌 수 있음.

- **api/v10/soccer/**  
  축구 도메인 REST API.
  - **player_router.py**: 선수 업로드(POST `/upload`), **임베딩 트리거**(GET `/embedding`, POST `/embedding/index`), 질의(POST `/query`)
  - **chat_router.py**: 챗 질의(POST `/query`) → ChatOrchestrator로 라우팅
  - **team_router.py**, **stadium_router.py**, **schedule_router.py**: 각각 팀/경기장/일정 업로드·처리
- **api/v10/admin/upload_router.py**  
  관리자용 업로드 라우팅.

즉, **어디서 기능이 노출되는지**는 `app/api/` 아래 라우터들을 보면 됩니다.

### 3.3 app/core/ — 설정·DB·LLM·RAG

- **config.py**  
  환경 변수 기반 설정(DATABASE_URL, Redis, 포트 등).  
  DB URL에서 `sslmode` 제거 등 asyncpg 호환 처리도 여기서 합니다.
- **database/**  
  - **session.py**: 비동기 DB 엔진·세션 생성, `get_db`, `AsyncSessionLocal`.  
    임베딩 배치·선수 조회는 모두 이 세션을 통해 이뤄집니다.
  - **base.py**, **mixin.py**: ORM 공통 베이스·타임스탬프 등 믹스인.
- **llm/providers/exaone_local.py**  
  ExaOne 로컬 모델 로드·LangChain 호환 LLM 생성.  
  MCP 서버에서 ExaOne 툴을 쓸 때 이 provider를 사용할 수 있습니다.
- **vectorstore.py**  
  pgvector 등 벡터 스토어 초기화( RAG 검색 시 사용).
- **rag_chain.py**  
  RAG 체인 구성(검색 + 생성).

**정리**: DB 연결·설정·ExaOne 로딩·RAG는 전부 `app/core/`에서 관리됩니다.

### 3.4 app/domain/ — 도메인 로직 (허브·스포크)

도메인은 **v10** 기준으로 구성되어 있고, **soccer**가 축구 도메인입니다.

#### 3.4.1 Hub(중앙) — app/domain/v10/soccer/hub/

**MCP 서버 (임베딩·ExaOne 툴 제공)**

- **hub/mcp/central_mcp_server.py**  
  - **역할**: 축구 도메인용 **중앙 MCP 서버**.  
    ExaOne(텍스트 생성·분석), KoELECTRA(임베딩·분류), 파일시스템, 통합 툴을 **툴**로 등록합니다.
  - **주요 툴 예**:
    - `koelectra_embed_text(text)` → 텍스트를 768차원 벡터로 변환
    - `koelectra_classify_text(text)` → 분류용 벡터
    - `exaone_analyze_player_data` 등 ExaOne 기반 분석 툴
    - 파일시스템(경로 조합, 파일 읽기 등), 통합 파이프라인 툴
  - **call_tool(tool_name, **kwargs)**  
    오케스트레이터가 툴 이름과 인자를 넘겨 호출하면, 여기 등록된 함수가 실행됩니다.
  - **모델 경로**:  
    ExaOne → `artifacts/base-models/exaone-2.4b`,  
    KoELECTRA → `artifacts/models--monologg--koelectra-small-v3-discriminator`  
    (실제 디렉터리는 프로젝트 루트 기준 상대 경로로 사용)

**오케스트레이터 (비즈니스 흐름·MCP 툴 호출)**

- **hub/orchestrators/player_orchestrator.py**  
  - 선수 업로드 후 **규칙/정책 기반** 처리, LangGraph 워크플로우
  - **임베딩 배치**: `trigger_embedding_migration()`  
    - DB에서 선수 목록 조회 → 각 선수마다 텍스트 조합 → **중앙 MCP의 `koelectra_embed_text` 툴 호출** → 나온 벡터를 `players_embeddings` 테이블에 저장  
  - 즉, **“MCP를 통해 임베딩을 자동으로 수행하는”** 코드는 **여기**에 있습니다.  
    ExaOne가 “임베딩 코드를 작성”하는 것이 아니라, **오케스트레이터가 MCP 툴(koelectra_embed_text)을 호출**해 임베딩을 생성합니다.
- **hub/orchestrators/chat_orchestrator.py**  
  사용자 질의를 받아 `QuestionClassifier`로 도메인(선수/팀/일정/경기장) 분류 후, 해당 오케스트레이터로 넘깁니다.
- **hub/orchestrators/team_orchestrator.py**, **stadium_orchestrator.py**, **schedule_orchestrator.py**  
  팀/경기장/일정 도메인 처리. 필요 시 중앙 MCP 툴(분석·임베딩 등)을 `_call_central_tool`로 호출합니다.

**리포지토리·라우팅**

- **hub/repositories/player_repository.py**  
  선수 CRUD, `list_all`, `count_all`, 배치 upsert 등.  
  임베딩 배치 시 “선수 목록 조회”는 이 리포지토리를 통해 이뤄집니다.
- **hub/routing/question_classifier.py**  
  질문 텍스트를 보고 어느 도메인(선수/팀/일정/경기장)으로 보낼지 결정.

**정리**:  
- **MCP가 “어디에” 정의되어 있나?** → `app/domain/v10/soccer/hub/mcp/central_mcp_server.py`  
- **임베딩이 “어디서” MCP를 쓰나?** → `app/domain/v10/soccer/hub/orchestrators/player_orchestrator.py`의 `trigger_embedding_migration()`  
- **ExaOne는?** → 같은 MCP 서버에 ExaOne 툴로 등록되어 있고, 오케스트레이터가 필요할 때 `call_tool`로 호출.

#### 3.4.2 Spokes(말단) — app/domain/v10/soccer/spokes/

- **spokes/infrastructure/embedding_client.py**  
  **로컬 Hugging Face 임베딩**(예: jhgan/ko-sroberta-multitask).  
  MCP KoELECTRA와 **별도** 경로로, `PlayerService.run_batch_indexing()`에서 사용합니다.  
  모델은 `artifacts/embedding-models/` 아래에 캐시됩니다.
- **spokes/services/player_service.py**  
  - 선수 JSONL 업로드 처리, 정규화, DB 저장
  - **run_batch_indexing(batch_size)**  
    DB에서 선수 조회 → `_create_player_content`로 텍스트 생성 → **EmbeddingClient**로 벡터 생성 → `players_embeddings`에 저장  
  - 즉, **MCP 없이** 로컬 임베딩 모델만 써서 선수 테이블을 벡터화하는 경로는 **여기**입니다.
- **spokes/agents/**  
  player_agent, team_agent 등 에이전트 정의. LangGraph 노드나 도메인별 세부 로직에서 사용될 수 있습니다.
- **spokes/tools/**  
  DB·공통 툴 등.

#### 3.4.3 모델·상태 정의 — app/domain/v10/soccer/models/

- **models/bases/**  
  - **players.py**, **player_embeddings.py**  
    선수 테이블, 선수 임베딩 테이블(768차원 벡터) 정의.  
  - 팀/경기장/일정/임베딩 테이블도 여기 있습니다.
- **models/states/**  
  LangGraph 등에서 쓰는 상태 스키마(예: player_state.py의 `PlayerState`, `PlayerProcessingState`).
- **prompts/player_embedding_react.md**  
  프롬프트 템플릿(현재는 비어 있을 수 있음).

### 3.5 app/alter_ollama/

Ollama용 임베딩 스키마/코드 생성 스크립트들.  
현재 서비스 경로는 MCP KoELECTRA 또는 EmbeddingClient 기준이라, 참고용에 가깝습니다.

---

## 4. MCP + ExaOne + 임베딩 — “어디서 무엇이 하는지”

### 4.1 흐름 요약

- **MCP**  
  `central_mcp_server.py`가 **툴**을 등록합니다.  
  - KoELECTRA: `koelectra_embed_text`, `koelectra_classify_text`  
  - ExaOne: 분석·생성용 툴  
- **임베딩이 “자동으로” 돌게 하는 쪽**  
  - **경로 1 (MCP·Redis)**  
    1. 프론트/클라이언트: GET `/api/v10/soccer/player/embedding`  
    2. `player_router.py` → Redis에 작업 등록 + 백그라운드 태스크로 `_run_embedding_migration_async` 실행  
    3. `PlayerOrchestrator().trigger_embedding_migration()`  
    4. 선수 조회 → 텍스트 조합 → **central_mcp_server.call_tool("koelectra_embed_text", text=content)**  
    5. MCP 서버가 KoELECTRA로 벡터 생성 후 반환  
    6. 오케스트레이터가 `players_embeddings`에 저장  
  - **경로 2 (로컬 EmbeddingClient)**  
    1. POST `/api/v10/soccer/player/embedding/index` 또는 `python scripts/run_player_embedding_batch.py`  
    2. `PlayerService().run_batch_indexing()`  
    3. 선수 조회 → 텍스트 조합 → **EmbeddingClient.get_embedding(content)** (MCP 미사용)  
    4. `players_embeddings`에 저장  

- **ExaOne**  
  ExaOne는 “임베딩 코드를 작성”하는 게 아니라,  
  MCP 서버에 **분석/생성 툴**로 등록되어 있고,  
  오케스트레이터나 에이전트가 `call_tool("exaone_analyze_...", ...)` 형태로 **호출**합니다.

### 4.2 관여 파일 정리

| 하고 싶은 일 | 관여 폴더/파일 |
|--------------|----------------|
| MCP 툴 정의(임베딩·ExaOne·파일 등) | `app/domain/v10/soccer/hub/mcp/central_mcp_server.py` |
| MCP 툴을 호출해 선수 임베딩 배치 | `app/domain/v10/soccer/hub/orchestrators/player_orchestrator.py` (`trigger_embedding_migration`) |
| Redis로 위 배치 트리거 | `app/api/v10/soccer/player_router.py` (GET `/embedding`), `app/api/shared/redis.py` |
| ExaOne 모델 로드 | `app/core/llm/providers/exaone_local.py`, `central_mcp_server.py`에서 사용 |
| 로컬 HF 모델로 선수 임베딩 (MCP 없이) | `app/domain/v10/soccer/spokes/infrastructure/embedding_client.py`, `spokes/services/player_service.py` (`run_batch_indexing`) |
| 선수 임베딩 API 노출 | `app/api/v10/soccer/player_router.py` (GET `/embedding`, POST `/embedding/index`) |

---

## 5. artfacts-training/ 폴더

KoELECTRA **분류** 모델의 학습 데이터 만들기·파인튜닝·임베딩 채우기 스크립트가 들어 있습니다.  
(폴더명은 "artifacts" 오타입니다.)

| 파일 | 역할 |
|------|------|
| **generate_training_data.py** | 축구 데이터(선수·팀·일정 등)로부터 KoELECTRA용 학습용 JSONL 생성 (라벨: BLOCK/POLICY_BASED/RULE_BASED 등) |
| **make_dataset_for_ko_electra.py** | (내용 비어 있을 수 있음) KoELECTRA용 데이터셋 가공 |
| **train.py** | KoELECTRA 시퀀스 분류 모델 파인튜닝. 설정·데이터 경로·출력 디렉터리 정의 |
| **fine_tunning.py** | 파인튜닝 예제(다른 설정/모델일 수 있음) |
| **ko_electra_embbedings.py** | KoELECTRA 임베딩 유틸(오타: embbedings) |
| **populate_player_embeddings.py** | DB 선수 조회 후 KoELECTRA로 임베딩 생성해 `PlayerEmbedding` 테이블에 채우는 **예제 스크립트**. (your_app 등 경로는 실제 프로젝트에 맞게 수정 필요) |
| **lang_graph_router.py** | LangGraph 라우팅 관련 (학습/실험용) |
| **koelectra_training_dataset.sft.jsonl**, **koelectra_training_reference.sft.jsonl** | 학습용 SFT 데이터 |
| **koelectra_orchestrator_finetuned/** | train.py 실행 후 저장되는 파인튜닝된 모델 체크포인트 |

**정리**:  
- “학습 데이터 생성” → `generate_training_data.py`  
- “KoELECTRA 파인튜닝” → `train.py`  
- “임베딩으로 DB 채우기” 예제 → `populate_player_embeddings.py` (실제 서비스는 app의 PlayerOrchestrator 또는 PlayerService 사용)

---

## 6. alembic/ 폴더

DB 스키마 **버전 관리(마이그레이션)** 를 담당합니다.

- **alembic.ini** (루트)  
  Alembic 설정 파일.  
- **alembic/env.py**  
  마이그레이션 실행 시 사용할 DB URL, `Base.metadata`(모델 메타데이터) 연결.  
  app의 모델을 import해 두어서, 테이블 추가/변경이 자동으로 감지되도록 합니다.
- **alembic/versions/*.py**  
  각 마이그레이션 버전(테이블 생성·컬럼 추가 등).  
  예: `ec4a26b7f4c9_create_embeddings_table.py` — 임베딩 테이블 생성.

**사용**:  
- `alembic upgrade head` → 최신 스키마 적용  
- `alembic revision --autogenerate -m "메시지"` → 모델 변경 반영한 새 마이그레이션 생성  

app 시작 시 `main.py`에서 `run_auto_migrations()`로 `alembic upgrade head`를 호출할 수 있도록 되어 있을 수 있습니다.

---

## 7. artifacts/ 경로 (디렉터리)

코드에서 **문자열 경로**로 참조하는 위치입니다. 실제 디렉터리는 첫 실행·다운로드 스크립트 등으로 만들어질 수 있습니다.

- **artifacts/base-models/exaone-2.4b**  
  ExaOne 로컬 모델 (central_mcp_server, exaone_local.py)
- **artifacts/models--monologg--koelectra-small-v3-discriminator**  
  KoELECTRA 모델 (central_mcp_server)
- **artifacts/embedding-models/**  
  EmbeddingClient가 쓰는 Hugging Face 임베딩 모델 캐시 (예: jhgan--ko-sroberta-multitask)

---

## 8. docs/architecture/ 폴더

- **mcp_structure_analysis.md**  
  MCP 아키텍처(선형 vs 스타), RAG 관점에서의 권장사항 정리.
- **PROJECT_STRUCTURE_LEARNING.md** (이 문서)  
  프로젝트 전체 구조·폴더/파일 역할·MCP·ExaOne·임베딩 흐름 학습용.

---

## 9. scripts/ 폴더

| 스크립트 | 역할 |
|----------|------|
| **run_player_embedding_batch.py** | `PlayerService().run_batch_indexing()` 실행 — 로컬 EmbeddingClient로 선수 전부 임베딩 후 DB 저장 |
| **check_player_embeddings.py** | `players_embeddings` 테이블 레코드 수·샘플 조회로 임베딩 완료 여부 확인 |
| **download_embedding_model.py** | Hugging Face 임베딩 모델을 `artifacts/embedding-models/`에 수동 다운로드 |
| **deploy.sh**, **setup-ec2.sh**, **install-systemd.sh**, **rag-app.service** | 배포·EC2·systemd 서비스 설정 |

---

## 10. 전체 요청 흐름 (읽기 순서)

1. **사용자** → 프론트 또는 curl  
2. **HTTP** → `app/main.py`에 등록된 라우터 (예: `app/api/v10/soccer/player_router.py`)  
3. **라우터** → 도메인 오케스트레이터 또는 서비스 호출  
   - 예: GET `/embedding` → Redis 등록 + `PlayerOrchestrator().trigger_embedding_migration()`  
   - 예: POST `/embedding/index` → `PlayerService().run_batch_indexing()`  
4. **오케스트레이터** → MCP 서버 `call_tool("koelectra_embed_text", ...)` 또는 **서비스** → `EmbeddingClient.get_embedding(...)`  
5. **MCP 서버** → KoELECTRA 모델 로드·추론 후 벡터 반환  
6. **오케스트레이터/서비스** → `players_embeddings`에 저장 (Repository/세션 사용)  
7. **DB** → PostgreSQL + pgvector (alembic으로 스키마 관리)

---

## 11. 학습 시 추천 순서

1. **진입점**: `app/main.py` → 어떤 라우터가 붙는지 확인  
2. **축구 API**: `app/api/v10/soccer/player_router.py` → 엔드포인트와 호출 대상(오케스트레이터/서비스) 파악  
3. **MCP 툴 정의**: `app/domain/v10/soccer/hub/mcp/central_mcp_server.py` → 어떤 툴이 있고, ExaOne/KoELECTRA가 어떻게 쓰이는지  
4. **임베딩 배치 (MCP 경로)**: `app/domain/v10/soccer/hub/orchestrators/player_orchestrator.py`의 `trigger_embedding_migration`  
5. **임베딩 배치 (로컬 경로)**: `app/domain/v10/soccer/spokes/services/player_service.py`의 `run_batch_indexing`, `app/domain/v10/soccer/spokes/infrastructure/embedding_client.py`  
6. **DB·세션**: `app/core/database/session.py`, `app/domain/v10/soccer/models/bases/player_embeddings.py`  
7. **학습·파인튜닝**: `artfacts-training/train.py`, `generate_training_data.py`  
8. **마이그레이션**: `alembic/env.py`, `alembic/versions/` 중 하나 열어보기  

이 순서로 보면 “어느 기능이 어디에 구현되어 있는지”와 “MCP를 통한 ExaOne·임베딩 자동화가 어떤 파일들로 이뤄지는지”를 한 번에 따라가기 좋습니다.
